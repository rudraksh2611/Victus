"""
Victus morning briefing package.

Layout:
  ``victus/briefing/`` — script content (weather, news, segments)
  ``victus/speech/``   — TTS (Edge, SAPI)
  ``victus/ui/``       — desktop overlay process
  ``runtime_support.py`` — config, HTTP helpers, autostart logging
  ``startup_gate.py``  — delays, internet wait, mutex
"""
